extends layout
include mixins/_text_field

block content
  main.w-full.flex.flex-col.items-center.pt-10.px-1(
    class="md:p-10"
  )
    form.w-full.max-w-sm.bg-white.shadow-md.rounded.px-8.pt-6.pb-8(action='' method='POST')
      .mb-4
        +_text_field({
          name: 'title', placeholder: 'Enter a title...'
        })
      .mb-6
        +_text_field({
          name: 'body', placeholder: 'Enter your message body...', type: 'textarea'
        })

      button(
        class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
        type='submit'
      ) Post